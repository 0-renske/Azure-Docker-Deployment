export default function Payment(){
    async function handlePay() {
        alert("Pay me now!!!, I am hungry")
    }
    //Add a small bot that talks about sleeping on the streets.(concept)
    //Make the presentation in nextjs(concept)
    //Users can upload their own pdf to train their LLM.
    return(
        <div>
            <title>Payments</title>
        </div>
    );
}